# ~/.zshenv — the one file zsh always reads from $HOME. Keep minimal.
# Symlinked from the dotfiles repo; all other zsh config lives in $ZDOTDIR.
export ZDOTDIR="$HOME/.dotfiles/zdotdir"

# Editor
export EDITOR=vim
export VIMINIT="source $HOME/.dotfiles/vimrc"
